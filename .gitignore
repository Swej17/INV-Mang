# Agent session state — local to each machine, never shared
.codex/
.claude/*
!.claude/settings.json

# Superpowers scratch: ports, pids, tokens, generated HTML
.superpowers/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Windows: a bash 2>NUL redirect creates a literal file by this name
NUL
