{
  "name": "@simple-flame/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@simple-flame/domain": "workspace:*",
    "postgres": "3.4.7",
    "@simple-flame/persistence-postgres": "workspace:*"
  }
}
